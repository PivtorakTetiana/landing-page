header.header
    .header__top
        .container
            .contacts
                a.contact(href="tel:+380639999999")
                    i.fa.fa-mobile.contact__icon(aria-hidden="true")
                    span.contact__text +380639999999

                a.contact(href="mailto:pivtoraktatiana@gmail.com")
                    i.fa.fa-envelope.contact__icon.contact__icon--email(aria-hidden="true")
                    span pivtoraktatiana@gmail.com
                a.facebook-icon(href="facebook.com")
                    i.fa.fa-facebook(aria-hidden="true")
    .container         
        .header__bottom
            .logo: img.logo__picture(src="images/logo.png" alt="logo")
            nav.nav
                - var items = ['Home', 'About us', 'Our Aproach', 'Our Work']
                each item in items
                    if item === 'Home'
                        a.nav__link.nav__link__active(href="#")= item
                    else 
                        a.nav__link(href="#")= item
            a.button.header__button.button--red(href="#") Free Consultation 
    